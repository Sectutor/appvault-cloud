# Build & push appvault-central to ghcr on every push (and on v* tags).
# Push to master       -> ghcr.io/sectutor/appvault-central:{latest, sha-<short>}
# Push a v* tag        -> ghcr.io/sectutor/appvault-central:<tag>
# The .dockerignore keeps the licensing DB/logs OUT of the public image.
name: build-central-image
on:
  push:
    branches: [main, master]
    tags: ['v*']
  workflow_dispatch:

concurrency:
  group: ci-central-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Log in to ghcr
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: Build & push
        run: |
          IMG=ghcr.io/sectutor/appvault-central
          docker build -t "$IMG:latest" .
          docker push "$IMG:latest"
          docker tag "$IMG:latest" "$IMG:sha-${GITHUB_SHA::7}"
          docker push "$IMG:sha-${GITHUB_SHA::7}"
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            docker tag "$IMG:latest" "$IMG:${GITHUB_REF#refs/tags/}"
            docker push "$IMG:${GITHUB_REF#refs/tags/}"
          fi
